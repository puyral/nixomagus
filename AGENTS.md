Remember to look at the machine's hostname to know where to edit.

Use the `rebuild` command when rebuilding. It *is* an instanciation of @packages/rebuild parametrized for this machine. See `rebuild -h` for more info. Make sure to use `rebuild --no-sign`.
